import React from 'react';

const AddButton = props => {
    return (
        <div>
            <button className="ui button">
                Add task
            </button>
            <input />
        </div>
    );
}

export default AddButton;